name: SonarQube Scan

on:
  push:
    branches:
      - main

jobs:
  sonar:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '22'

      - name: Build project (modifica con il tuo sistema di build)
        run: |
          echo "Esegui qui il comando di build corretto"
      
      - name: Run SonarQube scan
        run: |
          echo "Esegui qui il comando per avviare SonarQube"
